# mini-Shell
A mini Unix shell developed in C for Linux that executes system commands using fork(), execvp(), and wait(). It supports built-in commands (cd, exit), signal handling (SIGINT), and pipe (|) functionality for inter-process communication.
